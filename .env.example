# ================================
# PUBLIC Environment Variables (exposed to browser)
# WARNING: Any NEXT_PUBLIC_* variable is visible in client-side code!
# ================================

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Solana Configuration  
NEXT_PUBLIC_SOLANA_NETWORK=devnet
NEXT_PUBLIC_SOLANA_RPC_HOST=https://api.devnet.solana.com
NEXT_PUBLIC_PLATFORM_WALLET=your_platform_wallet_address
NEXT_PUBLIC_ESCROW_PROGRAM_ID=your-program-id

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ================================
# SERVER-ONLY Environment Variables (never exposed to browser)
# CRITICAL: Never prefix these with NEXT_PUBLIC_
# ================================

# Supabase Server Keys (CRITICAL - KEEP SECRET)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Shipping API Keys (CRITICAL - KEEP SECRET)
SHIPPO_API_KEY=your-shippo-api-key
SHIPPO_WEBHOOK_SECRET=your-webhook-secret
EASYPOST_API_KEY=your-easypost-api-key

# Redis (Upstash) - For rate limiting & caching
UPSTASH_REDIS_REST_URL=your-upstash-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# Email Service
EMAIL_API_KEY=your-email-api-key
EMAIL_FROM=noreply@shoprefit.com

# Session Secrets (minimum 32 characters)
JWT_SECRET=your-jwt-secret-minimum-32-chars
SESSION_SECRET=your-session-secret-minimum-32-chars

# Database URL (if using direct connection)
DATABASE_URL=postgresql://user:password@host:port/database

# eBay API (OAuth - Application Keys from developer.ebay.com)
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=
EBAY_REDIRECT_URI=https://shoprefit.com/api/admin/ebay/callback
EBAY_ENVIRONMENT=PRODUCTION

# ================================
# Environment
# ================================
NODE_ENV=development